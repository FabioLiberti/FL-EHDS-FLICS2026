# FL-EHDS Terminal Interface Dependencies
# Install with: pip install -r terminal/requirements.txt

# Menu navigation (arrow keys support)
questionary>=2.0.0

# Progress bars with colors
tqdm>=4.66.0

# Rich tables and formatting (optional, enhances output)
rich>=13.0.0

# Cross-platform color support
colorama>=0.4.6

# Core dependencies (should already be installed with main framework)
numpy>=1.24.0
scipy>=1.11.0
torch>=2.0.0

# Plotting (for graph generation)
matplotlib>=3.8.0
